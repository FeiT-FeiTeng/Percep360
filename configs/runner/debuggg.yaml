# @package _global_
defaults:
  - /runner/default

runner:
  checkpointing_steps: 3000 #5000
  validation_steps: 1000000000 #10000
  lr_scheduler: constant_with_warmup
  enable_xformers_memory_efficient_attention: true
  working_folder: "."
  train_batch_size: 1
  num_workers: 2
